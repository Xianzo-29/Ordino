<Window x:Class="Ordino.View.Home"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ordino.View"
        mc:Ignorable="d"
        Title="Home" Height="450" Width="800">
    <Grid Background="White">
        <!--<TextBlock Text="Main Menu" 
            FontSize="30" 
            Foreground="Black" 
            FontWeight="Medium"
            HorizontalAlignment="Center"
            Margin="0,128,0,-58"/>
        <Button Content="Login"
        Background="Gray"
        Foreground="White"
        HorizontalAlignment="Center"
        Height="30" 
        Width="80"
        Margin="0,0,0,0"/>
        <Button Content="Content"
            Background="Gray"
            Foreground="White"
            HorizontalAlignment="Center"
            Height="30" 
            Width="80"
            Margin="0,100,0,0"/>

        <Button Content="Dashboard"
        Background="Gray"
        Foreground="White"
        HorizontalAlignment="Center"
        Height="30" 
        Width="80"
        Margin="0,198,0,0"/>-->

        <TextBlock Text="{Binding CurrentUser.UserName, UpdateSourceTrigger=PropertyChanged,
            StringFormat='Welcome or sumn, {0}'}"
            HorizontalAlignment="Center" FontSize="10" Margin="10,0,0,350"
                   Width="150" Foreground="Black"/>

        <DataGrid ItemsSource="{Binding homePageTests}" AutoGenerateColumns="True"
                  CanUserAddRows="True" 
                  Margin="0,100,0,0"
                  Grid.RowSpan="2"/>
    </Grid>
</Window>
